apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    app: {{ .Release.Name }}
spec:
  type: NodePort
  ports:
  - port: 15777
    nodePort: 32500
    protocol: UDP
    name: "query"
    targetPort: "query"
  - port: 15000
    nodePort: 32501
    protocol: UDP
    name: "beacon"
    targetPort: "beacon"
  - port: 7777
    nodePort: 32502
    protocol: UDP
    name: "game"
    targetPort: "game"
  selector:
    app: {{ .Release.Name }}